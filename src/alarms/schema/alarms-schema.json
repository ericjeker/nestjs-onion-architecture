{
  "openapi": "3.1.0",
  "info": {
    "title": "Create Alarm Schema",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "CreateAlarmRequestBody": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique identifier for the alarm."
          },
          "name": {
            "type": "string",
            "description": "Name of the alarm."
          },
          "severity": {
            "type": "string",
            "description": "Severity of the alarm created."
          }
        },
        "required": [
          "id",
          "name",
          "severity"
        ]
      }
    }
  }
}
